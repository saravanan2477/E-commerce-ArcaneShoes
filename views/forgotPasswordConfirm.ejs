<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
       
        body {
            background: url('/image/pngtree-d-illustration-of-conceptual-cybersecurity-measures-for-data-protection-and-privacy-image_3790463.jpg') no-repeat center center fixed;
            background-size: cover;
            font: 16px "Helvetica Neue", Helvetica, sans-serif;
            -webkit-font-smoothing: antialiased;
        }
        /* PANEL */

  
body::before {
    content: "";
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(201, 176, 176, 0.5); /* Adjust the opacity as needed */
    backdrop-filter: blur(1px); /* Adjust the blur effect as needed */
    z-index: -1; /* Place the overlay behind other content */
}
        .panel {
            background: transparent;
            margin: 40px auto;
            width: 300px;
            border: 1px solid #0404ca;
            border-radius: 3px;
            padding: 40px;
            box-shadow: 1px 3px #26272c;
        }

        .panel__avatar {
            background: #fff;
            border: 8px solid #46393d;
            width: 50px;
            height: 500px;
            border-radius: 50%;
            margin: 0 auto 40px;
            display: block;
        }

        /* INPUTS */

        .inputs__item {
            padding-bottom: 40px;
        }

        .inputs__label {
            color: #b11905;
            display: block;
        }

        .inputs__input {
            border: none;
            border-bottom: 1px solid #1237c9;
            background: rgba(255, 255, 255, 0.8);
            display: block;
            width: 100%;
            padding: 10px 0;
            font: 16px "Helvetica Neue", Helvetica, sans-serif;
            color: #a64c41;
            border-radius: 5px;
        }

        .inputs__input:focus {
            outline: none;
        }

        .inputs__item--new .inputs__label,
        .inputs__item--new .inputs__input {
            color: #a64c41;
        }

        .inputs__item--cta {
            text-align: center;
            padding-bottom: 0;
            padding-top: 20px;
        }

        /* BTN */

        .btn {
            border: none;
            background: #f54a34;
            color: #c8bda0;
            font-size: 20px;
            border-radius: 3px;
            padding: 12px 50px;
            cursor: pointer;
        }
        .inputs {
    background: transparent; /* Set the background color to transparent */
    margin: 0; /* Remove default margin */
    padding: 0; /* Remove default padding */
}
    </style>
</head>
<body>
    <div class="panel">
        <img class="anel__avatarp" src="/public/images/logo.png" alt="" />
        <form action="/updatePassword" method="post" class="inputs">
            <div class="inputs__item">
                <label for="newPassword" class="inputs__label">New Password</label>
                <br>
                <input type="password" class="inputs__input" id="newPassword" name="newPassword" placeholder="Enter new password" >
                <% if (errors && errors.newPassword) { %>
                    <p class="error-message"><%= errors.newPassword %></p>
                <% } %>
            </div>
            <div class="inputs__item inputs__item--new">
                <label for="new-password" class="inputs__label">Confirm Password</label>
                <input type="password" class="inputs__input" id="new-password" name="confirmPassword" placeholder="Re-enter new password" >
                <% if (errors && errors.confirmPassword) { %>
                    <p class="error-message"><%= errors.confirmPassword %></p>
                <% } %>
            </div>
            <div class="inputs__item inputs__item--cta">
                <input type="submit" class="btn" value="RESET" />
            </div>
        </form>
    </div>
</body>
</html>